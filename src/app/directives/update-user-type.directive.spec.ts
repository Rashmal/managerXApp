import { UpdateUserTypeDirective } from './update-user-type.directive';

describe('UpdateUserTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateUserTypeDirective();
    expect(directive).toBeTruthy();
  });
});
